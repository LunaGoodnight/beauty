import { Header } from "../common/Header";
import { LeftNav } from "../common/LeftNav";

const facelift = () => {
  return (
    <div>
      <Header />
      <LeftNav />
      臉要好看，別人才會看你的臉。
    </div>
  );
};

export default facelift;
